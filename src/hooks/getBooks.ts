import { useEffect } from "react";

export interface Book {
    id: number;
    title: string;
    picture: string;
    summary: string;
    length: number; 
    reading_diff: string; 
    avaible_amount: number;
    author_id: number;
}

const getBooks = () => {
    useEffect(() => {
        
    }

    );
    return {};
}

export default getBooks;