
describe('the user can do book interactions', () => {
    beforeEach(() => {
        cy.visit('http://localhost:5173/')
    })

    it('user borrows one or more books and see all books in userpage', () => {
    });

    it('user bookmark one or more books and see all books in userpage', () => {
    });

    it('user can see all books in userpage', () => {
    });

    //ETC ETC
})
